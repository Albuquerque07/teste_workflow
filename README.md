# teste_workflow
Apenas testando a automação do github actions

#Passo a passo
```git
git clone https://github.com/Albuquerque07/teste_workflow.git
```

```git
python -m venv .venv
```

```git
pip install -r requirements.txt
```

Pegar os códigos de tokens do presidente da república digital e renomear o ".env.example" para ".env" apenas

Essa .env é onde fia efetivamente as chaves de API e tals que precisa de segurança
A .venv é aquela que fica a configuração do ambiente(Que você cria nesse segundo passo aí).